// import { City } from './Cities';

export interface Destination {
    _id?: string;
    destinationName: string;
    city: string;
    country: string;
    price: number;
    description: string;
    stay: string;
    imageLinks: string;
    // cityId: City;
}
